// Single source of truth for the product version label shown in the UI
// (sidebar branding, login screen) — see V1.0 UX polish task. Update this
// one constant rather than scattering the literal string across components.
export const APP_VERSION = 'v1.0';
